// content.js

// Fonction pour manipuler la page web
function processPage() {
    // Vous pouvez ajouter ici des modifications à la page web actuelle
    // en utilisant JavaScript, par exemple, modifier le texte, les liens, etc.
    // Cet exemple montre simplement une alerte.
    
    //alert("Le script de ZServDownload est actif sur cette page.");
  }
  
  // Écoute de l'événement DOMContentLoaded pour garantir que la page est entièrement chargée
  document.addEventListener('DOMContentLoaded', function () {
    // Appel de la fonction pour traiter la page
    processPage();
  });
  